# Cài thêm cho môi trường phát triển/CI (không cần trên production):
#   pip install -r requirements.txt -r requirements-dev.txt
pytest
pytest-mock
ruff
ragas==0.2.15
datasets<3
langchain-google-vertexai
langchain-community==0.3.27
