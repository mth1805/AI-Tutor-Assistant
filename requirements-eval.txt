# Chỉ cần khi chạy các script trong evaluation/ — không cần cho app chính
# hay CI thường xuyên (nặng, gọi API thật, không nên chạy trên mọi PR).
#
# Pin RAGAS and its OpenAI LangChain adapter to the LangChain 0.3 dependency
# family used by the application. RAGAS does not constrain langchain-openai;
# without this pin, pip can select a recent adapter that requires Core 1.x.
# Keep evaluation dependencies out of the normal app/CI install (see ci.yml).
ragas==0.2.15
langchain-openai==0.2.14
datasets
