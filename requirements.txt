# --- UI ---
streamlit
streamlit-pdf-viewer

# --- Document parsing ---
pypdf              # thay cho PyPDF2 (đã ngừng bảo trì)
python-docx

# --- OCR cho PDF dạng ảnh scan (tùy chọn, tự bỏ qua nếu thiếu) ---
# Cần thêm gói hệ thống poppler-utils + tesseract-ocr — xem packages.txt
# (Streamlit Community Cloud tự đọc file này để cài qua apt).
pdf2image
pytesseract

# --- Database / vector store (PostgreSQL + pgvector) ---
psycopg[binary]    # driver Postgres v3, cần cho langchain-postgres
pgvector
langchain-postgres

# --- Retry/backoff (lưới an toàn cho batch embedding) ---
tenacity

# --- Embedding LOCAL (không tốn quota API) ---
langchain-huggingface
sentence-transformers
torch      
torchvision

# --- LLM (chat/agent) / Search ---
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0
ddgs            # DuckDuckGoSearchRun
cohere
langchain-cohere>=0.3.0

# --- LangChain / LangGraph core ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0
langchain-experimental>=0.3.0
langchain-classic>=0.1.0
langgraph

# --- Config ---
python-dotenv

# -- S3/AWS ---
boto3
